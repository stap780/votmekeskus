<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), :html => { class: 'form-signin' }) do |f| %>
<h2 class="form-signin-heading"><%= t('.sign_up', :default => "Регистрация") %></h2>
  <%= render "devise/shared/error_messages", resource: resource %>
  <%= f.input_field :name, class: 'form-control' , placeholder: "Ваше Имя"   %>
    <%= f.input_field :email, autofocus: true , class: 'form-control', placeholder: "Email" , required: true %>
    <%= f.input_field :password, class: 'form-control' , placeholder: "Password - (#{@minimum_password_length} characters minimum)" , required: true   %>
    <%= f.input_field :password_confirmation, class: 'form-control' , placeholder: "Password confirmation"   , required: true  %>

    <%= f.submit t('.sign_up', default: "Зарегистрироваться"), class: 'btn btn-success' %>
<% end %>
